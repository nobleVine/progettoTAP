language: java

sudo: true

services:
  - redis-server

cache:
  directories:
  - $HOME/.m2

jdk:
- oraclejdk8

script:
- mvn -f negozio/negozio-concreto/pom.xml install -Dmaven.javadoc.skip=true -B -V -Pjacoco coveralls:report